import { defaultOffset } from "./defaultOffset";
import { getRgba, normalizeColor } from "./normalizeColor";
function normalizeShadow(param) {
  var shadowColor = param.shadowColor, shadowOffset = param.shadowOffset, shadowOpacity = param.shadowOpacity, shadowRadius = param.shadowRadius, _getRgba, _ref = shadowOffset || defaultOffset, height = _ref.height, width = _ref.width;
  return {
    shadowOffset: {
      width: width || 0,
      height: height || 0
    },
    shadowRadius: shadowRadius || 0,
    shadowColor: normalizeColor(shadowColor, 1),
    shadowOpacity: shadowOpacity ?? (shadowColor ? (_getRgba = getRgba(shadowColor)) === null || _getRgba === void 0 ? void 0 : _getRgba.a : 1)
  };
}
export {
  normalizeShadow
};
//# sourceMappingURL=normalizeShadow.js.map
