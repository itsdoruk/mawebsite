import { normalizeValueWithProperty } from "./normalizeValueWithProperty";
function transformsToString(transforms) {
  return transforms.map(
    // { scale: 2 } => 'scale(2)'
    // { translateX: 20 } => 'translateX(20px)'
    // { matrix: [1,2,3,4,5,6] } => 'matrix(1,2,3,4,5,6)'
    function(transform) {
      var type = Object.keys(transform)[0], value = transform[type];
      return type === "matrix" || type === "matrix3d" ? "".concat(type, "(").concat(value.join(","), ")") : "".concat(type, "(").concat(normalizeValueWithProperty(value, type), ")");
    }
  ).join(" ");
}
export {
  transformsToString
};
//# sourceMappingURL=transformsToString.js.map
